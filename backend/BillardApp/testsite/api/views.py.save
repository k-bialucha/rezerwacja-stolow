from django.shortcuts import render
from django.templates.response import TemplateResponse

# Create your views here.
def video(request):
	myString = "this is python string"
	return TemplateResponse(request, 'video/index.html', { "myString": mystring })

